/**
 * Twitter/X share card. Re-uses the Open Graph image so the two stay in
 * sync from a single source. Next.js wires this into `twitter:image`.
 */
export { alt, size, contentType, default } from './opengraph-image';
